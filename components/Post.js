import Moment from "moment"

class Post extends React.Component {
    render () {
        const { list } = this.props;
        const regex = /(<([^>]+)>)/ig;

        return (
            <React.Fragment>
                
            </React.Fragment>
        )
    }
}

export default Post;